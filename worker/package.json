{
  "name": "nourt-support-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker that handles Nourt theme support-form submissions (email + auto-responder via Brevo).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put BREVO_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
